# Task 2 RED checkpoint

This temporary marker exists only to trigger CI from the branch head where `renderer/CMakeLists.txt` requires the not-yet-implemented `hh_asset_format` target. Remove it after the RED failure is captured.
